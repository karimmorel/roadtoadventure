{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouvel équipement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}
            <a href="{{ path('_index') }}">Liste des équipements</a>
{% endblock %}
