{% extends 'base.html.twig' %}

{% block body %}
    <h1>Panoply</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panoply.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ panoply.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ panoply.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('panoply_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('panoply_edit', { 'id': panoply.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
