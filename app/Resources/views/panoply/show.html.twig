{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ panoply.name }}</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panoply.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ panoply.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ panoply.description }}</td>
            </tr>
        </tbody>
    </table>

            <a href="{{ path('panoply_index') }}">Retour aux panoplies</a><br/>
            <a href="{{ path('panoply_edit', { 'id': panoply.id }) }}">Modifier</a><br/>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer"><br/>
            {{ form_end(delete_form) }}
{% endblock %}
