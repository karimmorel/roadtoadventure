{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ category.name }}</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
            <a href="{{ path('category_index') }}">Liste des catégories</a><br/>
            <a href="{{ path('category_edit', { 'id': category.id }) }}">Modifier</a><br/>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
{% endblock %}
